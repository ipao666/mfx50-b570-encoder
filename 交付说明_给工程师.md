# MFX50 SDK 交付说明

日期：2026-05-29
工程名：MFX50 B570 HEVC 编码 SDK
源码目录：`mfx50_dll_src`

## 1. 这是什么

这是给工程软件调用的 B570 H.265/HEVC 编码封装库。

当前包含两套接口：

```text
libmfx50_encoder.so
  离线/压测接口，主要用于输入文件列表、多路压测。

libmfx50_realtime.so
  实时逐帧接口，当前已改成 native oneVPL：
  PushPacket -> oneVPL decode -> oneVPL encode -> PollPacket/callback 输出。
```

当前实时库不是旧的 FIFO + sample_multi_transcode 封装；`MFX50RT_PushPacket()` 已经走 native oneVPL。

后续业务实时接入主线只使用 `MFX50RT_*`。`MFX50_*` / `libmfx50_encoder.so` 只作为 benchmark 工具保留，用来证明真实多路并发能力，不作为实时业务 SDK。

## 2. 目录内容

```text
CMakeLists.txt
mfx50_encoder.h/.cpp          离线/压测 C API
mfx50_realtime.h/.cpp         实时逐帧 C API
demo_mfx50_benchmark.cpp      离线/压测 demo
python/                       Python ctypes 示例
docs/                         接口说明、实现状态、测试结果
prebuilt/                     如果交付包内有，表示本机预编译 .so 和 demo
```

## 3. 运行环境

目标机器需要：

```text
Ubuntu 原生 Linux x86_64
Intel B570 / Arc 驱动
Intel media driver
oneVPL runtime / libvpl
libva / libva-drm
/dev/dri/renderD129 或 renderD128 可访问
```

运行用户需要在 `render` 组，能访问：

```text
/dev/dri/renderD129
/dev/dri/renderD128
```

## 4. 编译

```bash
cd mfx50_dll_src
cmake -S . -B build
cmake --build build -j$(nproc)
```

编译产物：

```text
build/libmfx50_encoder.so
build/libmfx50_realtime.so
build/demo_mfx50_benchmark
build/demo_mfx50_realtime_file
build/mfx50rt_stream_probe
build/mfx50rt_api_probe
```

可选安装到指定目录：

```bash
cmake --install build --prefix /opt/mfx50rt
```

安装后可用 pkg-config：

```bash
PKG_CONFIG_PATH=/opt/mfx50rt/lib/pkgconfig \
pkg-config --cflags --libs mfx50_realtime
```

如果交付包里带了 `prebuilt/`，可以先用预编译库快速试，但正式集成建议在目标机器重新编译。

## 5. 实时接口怎么用

核心头文件：

```c
#include "mfx50_realtime.h"
```

启动时先打印版本和 ABI，便于现场确认库和头文件匹配：

```c
printf("mfx50rt version=%s abi=%d\n",
       MFX50RT_GetVersion(),
       MFX50RT_GetAbiVersion());
```

最小流程：

```c
MFX50RT_Config cfg;
memset(&cfg, 0, sizeof(cfg));
cfg.struct_size = sizeof(cfg);
MFX50RT_DefaultConfig(&cfg);

cfg.input_mode = MFX50_INPUT_ENCODED_PACKET;
cfg.output_mode = MFX50_OUTPUT_POLL;
cfg.input_codec = MFX50_CODEC_H264;
cfg.output_codec = MFX50_CODEC_HEVC;
cfg.profile = MFX50_PROFILE_QUALITY_90_NEAR;
cfg.device_selector = "auto";
cfg.route_count = 1;
cfg.fps_num = 25;
cfg.fps_den = 1;

MFX50RT_Handle h = NULL;
MFX50RT_Create(&cfg, &h);

MFX50RT_Packet pkt = {0};
pkt.struct_size = sizeof(pkt);
pkt.stream_id = 0;
pkt.data = h264_data;
pkt.size = h264_size;
pkt.pts = pts;
MFX50RT_PushPacket(h, &pkt);

uint8_t out_buf[8 * 1024 * 1024];
MFX50RT_EncodedPacket out = {0};
out.struct_size = sizeof(out);
out.data = out_buf;
out.capacity = sizeof(out_buf);

while (MFX50RT_PollPacket(h, &out) == MFX50_OK) {
    // out.data / out.size 是 HEVC 输出
}

MFX50RT_Flush(h);
MFX50RT_Close(h);
```

错误码用 `MFX50RT_StatusString(rc)` 转成字符串，详细错误用 `MFX50RT_GetLastError(h)`。`PollPacket` 当前是调用方 buffer 模式：调用方传入 `out.data/out.capacity`，SDK 只 copy 输出，不分配输出包，因此不需要 `ReleasePacket`。如果返回 `MFX50_ERR_BUFFER_TOO_SMALL`，`out.size` 是所需容量，扩容后重试。更完整规则见 `docs/错误码说明.md` 和 `docs/线程与内存归属说明.md`。

多路输入时，设置：

```c
cfg.route_count = 50;
pkt.stream_id = 0..49;
```

## 6. 当前内置 profile

```text
MFX50_PROFILE_THROUGHPUT_ONLY
  速度优先：QP32 / GOP60。

MFX50_PROFILE_QUALITY_90_NEAR
  当前质量优先候选：
  TU4 + GOP300 + B4 + BRef pyramid + QPI/QPP/QPB=33/38/45。
  参考结果：约 16.32%，SSIM 基本没变。

MFX50_PROFILE_COMPRESS_85_PROBE
  当前 85% 压缩候选：
  TU4 + GOP300 + B4 + BRef pyramid + QPI/QPP/QPB=33/39/44。
  参考结果：约 14.66%，SSIM avg 约 0.9016，p5/min 略低于 0.88 线。
```

TU2 不作为主线 profile。需要诊断时可手动：

```c
cfg.target_usage = 2;
```

## 7. Python 快速测试

C/C++ 工程师优先使用文件级 demo 验证实时接口闭环：

```bash
./build/demo_mfx50_realtime_file \
  /path/to/input.h264 \
  /tmp/out.hevc \
  quality \
  25 \
  auto
```

验证：

```bash
ffprobe -v error -select_streams v:0 \
  -show_entries stream=codec_name,width,height \
  -of default=noprint_wrappers=1 /tmp/out.hevc
```

更完整的实时 packet 诊断使用 `mfx50rt_stream_probe`：

```bash
LD_LIBRARY_PATH=build \
./build/mfx50rt_stream_probe \
  --input /path/to/input.h264 \
  --output /tmp/out.h265 \
  --device /dev/dri/renderD129 \
  --fps 25 \
  --profile quality \
  --strict
```

重点看：

```text
ffprobe_ok=1
ffprobe_codec=hevc
ffprobe_keyframes > 0
sdk_dts_nonmonotonic=0
```

基础 C ABI 诊断使用 `mfx50rt_api_probe`，不需要 B570：

```bash
LD_LIBRARY_PATH=build ./build/mfx50rt_api_probe
```

```bash
PYTHONPATH=python python3 - <<'PY'
from pathlib import Path
from mfx50_realtime import MFX50RealtimeEncoder, MFX50_PROFILE_QUALITY_90_NEAR

inp = Path("/path/to/input.h264")
out = Path("/tmp/out.hevc")

with MFX50RealtimeEncoder("./build/libmfx50_realtime.so",
                          profile=MFX50_PROFILE_QUALITY_90_NEAR,
                          fps_num=25) as enc:
    enc.push_packet(0, inp.read_bytes(), pts=0)
    enc.push_packet(0, b"", end_of_stream=True)
    with out.open("wb") as f:
        while True:
            pkt = enc.poll_packet()
            if pkt is None:
                break
            _, data, _, _ = pkt
            f.write(data)
PY
```

验证：

```bash
ffprobe -v error -select_streams v:0 \
  -show_entries stream=codec_name,width,height \
  -of default=noprint_wrappers=1 /tmp/out.hevc
```

## 8. 当前限制

实时库 v0.4.0 当前先跑通 native oneVPL 实时基础链路，并补了版本、ABI、错误码字符串、日志回调和基础 stats。

默认还没有打开：

```text
scene_analyzer
MBQP
pre-denoise
smooth-scale
scene-frame-type-control
ROI sidecar
逐帧 force IDR
```

这些算法下一步会插在：

```text
DecodeFrameAsync 得到 surface 后
EncodeFrameAsync 提交 surface 前
```

## 9. 工程师接入建议

第一步只接实时基础 API：

```text
MFX50RT_Create
MFX50RT_PushPacket
MFX50RT_PollPacket 或 MFX50RT_SetOutputCallback
MFX50RT_Flush
MFX50RT_Close
```

不要直接调用 oneVPL，也不要直接改编码参数。业务层只选 profile。

## 10. 已验证状态

本机已验证：

```text
build_native_rt/libmfx50_realtime.so 构建成功
单路 H.264 输入 -> HEVC 输出成功
decoded_frames=248
encoded_frames=248
ffprobe: hevc 1920x1080
B 帧结构已出现
```
