# 源码干净版说明

这个文件夹是从 `D:\mfx50_core_code_20260531\combination` 单独提取出来的源码快照，方便后续继续开发、检查和重新打包 SDK。

## 包含内容

- `src/`：核心算法、policy、oneVPL 适配和运行时源码。
- `include/`：公开 C ABI 头文件。
- `examples/`：示例程序源码和 B570 多路运行脚本。
- `tools/`：C++ 能力查询/接口探针工具源码。
- `tests/`：C/C++ 单元测试和接口测试源码。
- `packaging/`：二进制 SDK 打包脚本。
- `docs/`：当前版本必要文档，不含历史调参流水账。
- `configs/`、`python/`、`demo/`：配置、Python 绑定和 demo。
- 根目录 `CMakeLists.txt`、`mfx50_realtime.*`、`mfx50_encoder.*`：构建入口和实时/编码器相关源码。

## 不包含内容

- `deliverables/`：最终二进制交付包。
- `backups/`：历史备份。
- `tools/codex_archive_20260602/`：历史调参探针归档。
- 评估输出、SSIM 临时结果、测试视频。
- `.tar.gz`、`.zip`、`.mp4`、`.h264`、`.h265`、`.yuv` 等二进制或视频文件。
- `.pyc`、`.csv` 等临时/结果文件。

## 主要入口

- SDK 打包脚本：`packaging/make_b570_binary_sdk.sh`
- 工程师交付说明：`docs/ENGINEER_HANDOFF.md`
- Policy API：`include/mfx50_policy.h`、`include/mfx50_types.h`
- 实时接口：`mfx50_realtime.h`
- B570 多路运行脚本：`examples/b570_fastpath/`
